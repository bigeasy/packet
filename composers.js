var __slice = [].slice
var $ = require('programmatic')

throw new Error

function line () {
    return __slice.call(arguments).join('')
}

function str (value) {
    return JSON.stringify(value)
}

var count = 0
